
import { ReactNode } from "react";

export const metadata = {
  title: "Matches - Sports Predictions",
  description: "Live and upcoming sports matches across NFL, NBA, MLB, and Soccer",
};

export default function MatchesLayout({ children }: { children: ReactNode }) {
  return children;
}
